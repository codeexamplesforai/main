Hi [Senior Leader's Name],

As we kick off the G.A.M.M.A Detection Framework POC, I'd like to request an increase in the Claude Code monthly budget for the delivery team — from the current $250/month/developer to $1,000/month/developer — and, preferably, that this be provisioned as a shared pool across the team rather than per-seat caps.

Why this is needed:

1. Leaner team, bigger throughput: Our plan delivers the output of 15–17 developers with a squad of 4–5, using an AI delivery engine built on Claude Code. Each developer orchestrates multiple AI agents in parallel, so token consumption scales with agent throughput, not headcount — the current per-seat cap is sized for one developer's AI-assisted coding, not one developer driving a fleet of agents. This engine underpins the ~30–35% efficiency gain committed in the kick-off (333 human man-days across 162 modules, 16-week plan).

2. We are already running the most cost-optimised setup: We deliberately bifurcate work across models so every token is spent where it has the most leverage. Claude Opus handles high-value reasoning — reading our FSD/TSD docs and generating structured prompt series (15–25 self-contained prompts per task), deep code review, and complex fixes. Claude Sonnet covers routine reviews and quick bug fixes. The bulk implementation tokens then run on Qwen (via opencode), guided step-by-step by those Opus-generated prompts with test cases, a 00_README knowledge base and a _STATE.md memory file. This combination cuts premium Opus-class token usage by an estimated ~65–70% and total token spend by ~55–60% versus an Opus-only setup — the uplift we're requesting is on top of an already heavily optimised consumption model, not a substitute for one.

3. Momentum protection: Usage is bursty by build phase — a developer running a heavy band (e.g. the Model Plane) may need 2–3× a quiet seat's allowance in a given week. Hitting a cap mid-sprint stalls an entire prompt-series pipeline and the humans reviewing it, a hard blocker on our 16-week critical path. This is why a pooled allowance is preferable.

4. No downside to headroom: The budget is a ceiling, not a spend commitment. If the max allowed balance is never reached, unused headroom simply lapses — benefit only. Actual consumption will be tracked per band/module and reported to SteerCo, including realized savings.

Team members for whom the limit increase is requested:


Happy to walk you through the detailed justification (it's also covered in the kick-off deck, AI-Accelerated Delivery section). Given the aggressive timeline, an early approval would help us avoid any interruption once the build bands ramp up.
